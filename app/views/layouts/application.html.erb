<!DOCTYPE html>
<html>
  <head>
    <title>Revelinho</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.0/css/all.css" integrity="sha384-lZN37f5QGtY3VHgisS14W3ExzMWZxybE1SJSEsQp9S+oqd12jhcu+A56Ebc1zFSJ" crossorigin="anonymous">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class='navbar navbar-expand-lg navbar-light bg-light py-3 border-bottom'>
      <%= link_to image_tag('revelinho.svg', size: '250x50'), root_path %>
      <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="conteudoNavbarSuportado" aria-expanded="false" aria-label="Alterna navegação">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
          <% if candidate_signed_in? %>
          <li class='nav-item'>
            <%= link_to 'Logout', destroy_candidate_session_path, method: :delete, class: 'nav-link p-3' %>
          </li>
          <% elsif employee_signed_in? %>
          <li class='nav-item'>
            <%= link_to 'Logout', destroy_employee_session_path, method: :delete, class: 'nav-link p-3' %>
          </li>

          <% else %>
          <li class='nav-item'>
            <%= link_to 'Login de funcionário', new_employee_session_path, class: 'nav-link p-3' %>
          </li>
          <li class='nav-item'>
            <%= link_to 'Login de candidato', new_candidate_session_path, class: 'nav-link p-3' %>
          </li>
          <% end %>
        </ul>
      </div>
    </nav>

    <%= @application_presenter.flash_alerts %>

    <%= yield %>

    <%= yield :javascript %>

  </body>
</html>
