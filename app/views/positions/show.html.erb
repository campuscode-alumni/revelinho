<div class="container-fluid">
  <div class="row">
    <div class="col-md-12">
      <div class="page-header">
        <h1>
          <%= @position.title %> <small>@ <%= @position.company.name %></small>
        </h1>
      </div>
        <ul>
          <li><%= @position.industry %></li>
          <li><%= @position.description %></li>
          <li>Salário de: <%= @position.salary_from %></li>
          <li>até: <%= @position.salary_to %></li>
      </ul>
    </div>
  </div>
  <div class="container mt-5">
    <div class="row justify-content-center">
      <%= link_to t('messages.go_back'), :back, class: 'btn btn-outline-primary' %>
    </div>
  </div>
</div>
