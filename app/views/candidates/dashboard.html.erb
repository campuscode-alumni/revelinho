<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="jumbotron">
                <h2>
                    <%= "Olá fulano" %>
                    <%= "Seu perfil está ativo!" if current_candidate.published? %>
                </h2>
                <p>
                    This is a template for a simple marketing or informational website. It includes a large callout called the hero unit and three supporting pieces of content. Use it as a starting point to create something more unique.
                </p>
                <% if current_candidate.published? %>
                <%= link_to 'Editar Perfil', edit_candidate_profile_path(current_candidate), class: "btn btn-primary btn-large" %>
                <% else %>
                <%= link_to 'Concluir perfil', new_candidate_profile_path, class: "btn btn-primary btn-large" %>
                <% end %>
            </div>
        </div>
    </div>
</div>
